<%= form_with(model: po_item) do |form| %>
  <% if po_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(po_item.errors.count, "error") %> prohibited this po_item from being saved:</h2>

      <ul>
        <% po_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name %>
    <%= form.number_field :name %> 
  </div>

  <div>
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
