<p style="color: green"><%= notice %></p>

<div class="container">
  <h1>Items</h1>

  <div id="items">
    <% @items.each do |item| %>
      <h5><%= item.name %></h5>
      <p><%= item.bid %></p>
      <p>
        <%= link_to "Show this item", item %>
      </p>
    <% end %>
  </div>

  <%= link_to "New item", new_item_path %>

<div>
